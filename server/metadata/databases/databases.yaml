- name: silhouettes
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://postgres:pgpassword@postgres:5432/postgres
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include silhouettes/tables/tables.yaml"
